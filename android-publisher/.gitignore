# Gradle cache
.gradle/

# Local SDK path (machine-specific, generated by Android Studio)
local.properties

# Build outputs (also caught by root build/)
/app/build/

# Release signing keys — never commit
*.jks
*.keystore

# APK/AAB artifacts
*.apk
*.aab